namespace UI
{
	/// <summary> For now it just reloads the scene just like <see cref="StartGameButton"/> cause its still light enough. </summary>
	public class RestartGameButton : StartGameButton
	{
	}
}